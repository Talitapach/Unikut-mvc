package Codes.Model.entities;

public class Common extends User{


    public Common(String name, String userName, String password, boolean admin) {
      super(name, userName, password, admin);
    }

    
}
